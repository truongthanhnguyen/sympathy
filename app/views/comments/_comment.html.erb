<div class="col-md-10 col-md-offset-1 comment" id="comment-<%= comment.id %>">
  <%= link_to comment.user_name %><br>
  <%= comment.content %><br>
  <span class="timestamp">
    <%= t "comment.created_at" %> <%= timeago(comment.created_at) %>
  </span>
</div>
