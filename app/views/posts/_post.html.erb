<li id="post-<%= post.id %>">
  <span class="user"><%= link_to post.user.name, post.user %></span>
  <span class="content">
    <%= post.content_type %>
  </span>
  <span class="timestamp">
    <%= t "post.posted" %><%= time_ago_in_words(post.created_at) %><%= t "post.ago" %>
  </span>
</li>
