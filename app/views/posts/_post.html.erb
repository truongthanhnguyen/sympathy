<li id="post-<%= post.id %>">
  <span class="user"><%= link_to post.user_name, post.user %></span>
  <span class="content">
    <%= post.content_type %>
  </span>
  <span class="timestamp">
    <%= t "post.posted" %> <%= timeago(post.created_at) %>
	</span>
</li>
